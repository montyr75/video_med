name: VideoMed
description: Server, admin, and client apps for controlling client video playlists.
dependencies:
  args: any
  html_components: any
  polymer: ">=0.10.0-pre.4 <0.11.0"
  polymer_elements: any
  route: any
transformers:
- polymer:
    entry_points:
    - web/index.html
    - web/admin.html
